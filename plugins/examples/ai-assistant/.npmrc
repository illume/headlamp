# Required for file: dependencies (e.g. @headlamp-k8s/ai-common, ai-ui) to be
# installed as real copies rather than symlinks, which ensures npm ci works
# correctly with workspace packages referenced via file: protocol.
install-links=true
